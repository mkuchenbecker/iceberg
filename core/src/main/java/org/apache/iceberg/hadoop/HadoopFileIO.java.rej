diff a/core/src/main/java/org/apache/iceberg/hadoop/HadoopFileIO.java b/core/src/main/java/org/apache/iceberg/hadoop/HadoopFileIO.java	(rejected hunks)
@@ -97,6 +97,11 @@ public class HadoopFileIO implements HadoopConfigurable, DelegateFileIO {
     return HadoopOutputFile.fromPath(new Path(path), hadoopConf.get());
   }
 
+  @Override
+  public OutputFile newOutputFile(String path, Map<String, String> fileProperties) {
+    return HadoopOutputFile.fromPath(new Path(path), hadoopConf.get(), fileProperties);
+  }
+
   @Override
   public void deleteFile(String path) {
     Path toDelete = new Path(path);
